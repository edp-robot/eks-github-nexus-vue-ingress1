apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vue-ingress1.fullname" . }}-test-connection"
  labels:
    {{- include "vue-ingress1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vue-ingress1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
